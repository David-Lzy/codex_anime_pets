# Proportion Review

The first production sequence requested semi-chibi proportions but rendered visibly longer legs. It is retained as the anime variant (`assistant-004-anime`), not advertised as the compact 3.5-head design. Original prompts remain unchanged as provenance. The compact variant receives its own shorter-body reference and drawings.

`anime-first-idle` preserves the unused earlier anime candidate. It is not a runtime animation or release input.
